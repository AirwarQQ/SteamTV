# SteamTV

Утилита для Windows: следит за подключением геймпада и запуском **Steam Big Picture**.
При подключении геймпада будит ТВ по ADB, включает целевой дисплей, переключает вход ТВ
на HDMI (ПК) и запускает Big Picture. При закрытии Big Picture — гасит дисплей, возвращает
источник ТВ и сворачивает Steam. C#-порт старого PowerShell-скрипта.

## Структура

```
SteamTV/
├─ Program.cs              — точка входа (single-instance, флаг -autostart)
├─ AppSettings.cs          — настройки в реестре HKCU\Software\SteamTV
├─ Interop/
│  └─ Native.cs            — P/Invoke: окна + Windows CCD display API
├─ Services/
│  ├─ DisplayManager.cs    — вкл/выкл/восстановление мониторов (CCD)
│  ├─ Adb.cs               — обёртка над adb.exe
│  ├─ SteamHelper.cs       — геймпад (WMI), Big Picture, Steam
│  └─ MonitorService.cs    — фоновый цикл (вся логика)
├─ Ui/
│  └─ MainForm.cs          — окно: IP, дисплей, чекбоксы, журнал, трей
├─ SteamTV.csproj          — для `dotnet build` / Visual Studio
└─ build.bat               — сборка одним csc.exe, без SDK
```

## Сборка

**Вариант 1 — через SDK / VS Code:**
```
dotnet build -c Release
```

**Вариант 2 — без SDK** (нужен только .NET Framework 4.x, он есть в Windows):
запусти `build.bat`. Внутренних зависимостей и NuGet-пакетов в рантайме нет.

Значок: положи `start_icon.ico` рядом и раскомментируй строку `ApplicationIcon`
в `.csproj` (или `ICON` в `build.bat`).

## Что нового по сравнению со скриптом

- **Поле IP телевизора** в окне (раньше `192.168.1.102` было захардкожено).
- **Галочка «Отключать остальные мониторы»**: при активации остаётся только ТВ,
  при выходе из Big Picture прежняя раскладка мониторов восстанавливается.
- Настройки и пути вынесены в реестр — меняются без перекомпиляции.
- Журнал событий в окне, сворачивание в трей, опциональный автозапуск с Windows.

## Настройки

Хранятся в `HKCU\Software\SteamTV` (IP, номер дисплея, флаг отключения мониторов,
а также пути к `adb.exe`/`steam.exe` и имена Android-пакетов). Значения по умолчанию
взяты из старого скрипта — при необходимости поправь пути к adb/Steam прямо в реестре.

Автозапуск (галочка в окне) пишет значение `SteamTV_Controller` в
`HKCU\...\CurrentVersion\Run` со строкой `"…\SteamTV.exe" -autostart` — приложение
стартует свёрнутым в трей и сразу включает монитор.

## Важно про «номер дисплея»

«Целевой дисплей №» — это номер из «Параметров экрана» Windows (`\\.\DISPLAYn`).
Нажми кнопку **«Дисплеи…»** в окне — она покажет список (номер / имя / активен ли),
чтобы выбрать правильный номер телевизора. Повторное включение по номеру работает,
пока монитор физически подключён (телевизор остаётся на HDMI, просто отключён в Windows).

## Проверка переключения дисплеев (Tools/DisplayTest)

Отдельный консольный тестер, который дёргает те же функции, что и приложение
(`DisplayManager.EnableDisplay` / `DisableDisplay`), и печатает «сырую» таблицу
путей CCD — удобно отлаживать включение отключённого в Windows монитора.

Сборка (из корня проекта): запусти `build-displaytest.bat` (нужен только .NET
Framework, без SDK). Затем:

```
DisplayTest list     показать дисплеи и таблицу путей
DisplayTest on 3     включить дисплей №3
DisplayTest off 3    выключить дисплей №3
```

Если включение даёт ошибку (например `87 — Invalid paths information`), вывод
`list` покажет, как именно отключённый монитор виден системе (srcId/tgtId/имя) —
по этим данным правим стратегию в `DisplayManager`.